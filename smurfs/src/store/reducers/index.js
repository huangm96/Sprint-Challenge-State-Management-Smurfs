

const initialState = {
  name: "",
    age: null,
    height: null,
    id:null,
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
